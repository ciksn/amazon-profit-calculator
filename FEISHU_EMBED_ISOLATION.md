# 飞书网页卡片独立实例说明

普通网页卡片预览模式不会可靠地向网页提供当前云文档 ID，因此普通卡片采用“项目专属链接”完成绑定；`feishu-docs-addon/` 云文档小组件仍可用于自动创建空白实例。

## 当前方案

1. 归属人登录计算器，创建或选择当前选品文档对应的产品。
2. 产品默认开启公司内共享，根据需要点击“复制只读链接”或“复制编辑链接”。
3. 把所选权限的专属链接作为网页卡片插入对应的飞书选品文档。
4. 只读链接只能查看；编辑链接允许公司成员协作修改。两者都只展示这一项产品，不会显示归属人的产品列表。
5. 如需停止分享，归属人在个人计算器中把该产品的“共享：开”切换为“共享：关”。

专属访问码位于 URL 的 `?share=...` 参数中。访问者仍须通过公司飞书鉴权；访问码只定位到单个产品，不能列出归属人的其他数据。链接应跟随飞书文档权限管理，不应发布到公开渠道。

## 安全边界

- 共享嵌入接口不会返回归属人的品类列表。
- 使用只读链接的成员只能读取和计算，所有保存、导入、分析和删除请求均由服务端拒绝。
- 只读访问码和编辑访问码彼此独立；只读响应不会返回编辑访问码。
- 卡片顶部同时显示项目归属人、当前飞书用户和当前权限。
- 缺少访问码、访问码错误时直接拒绝，不会回退到数据库第一条数据。
- 后端从访问码确定项目，忽略前端提交的其他项目 ID。
- 竞品和方案记录会校验是否属于当前实例。
- 嵌入卡片不能删除整个实例。

## 后续升级

云文档小组件会把测算绑定保存到飞书 `Record`，并校验当前 `docToken + blockId`。模板复制后上下文变化，组件自动创建新的空白实例，不需要用户复制链接。构建与发布步骤见 `feishu-docs-addon/README.md`。
